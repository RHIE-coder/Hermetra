# 게이트 기록 — 2026-08-18 · workbench-guide-refresh

같은 날 두 번째 회차. 앞 회차(`workbench-guide-bilingual`)가 남긴 결함 하나를 잡는다.

| 슬롯 | 값 |
|---|---|
| 대상 | `src/main/services/past-guides.ts`(신규) · `src/main/services/scripts.ts` |
| 인수조건 | `AC-studio.browser-41`(신규) |
| 스펙 | `docs/spec/studio/browser.md` |
| 테스트 | `tests/api/scripts.test.ts` (+3, 기존 1건 강화) |
| 새 의존성 | 없음 |

## 제보

`.harness/feedback/20260818-164608-studio-browser` — 편집기의 `GUIDE_ko.md` 76~91줄을 두르고
*"아직도 남아있는데...? 왜 거짓말쳤어. 없앴다고"*. 몇 시간 전에 지웠다고 보고한
`export extract` / `export transform` 절이다.

## 원인 — 코드는 지웠는데 파일이 남았다

앱이 쥔 가이드 원문에는 그 절이 없다(0건). 남은 것은 **디스크의 파일**이다.

`ensureGuide` 는 "없으면 쓰고, 있으면 손 안 댄다"였다. 시작 파일(`example.ts`)에는 이미 있는
장치 — 앱이 쓴 바이트와 같으면 새 것으로 바꾸고, 사람이 한 줄이라도 고쳤으면 그대로 둔다 —
가 가이드에는 없었다. 코드 주석은 "개정판을 낼 때 필요해질 것"이라고 적고 있었고, 그날이 몇
시간 뒤였다.

그 파일이 사람이 고친 것이 아니라 앱이 쓴 옛 판임을 바이트로 확인했다: 한국어판은 앞 1977자가
지금 판과 글자 단위로 같고 거기서부터 옛 절이 붙어 있다(영어판 2844자).

## 고친 것

- **`services/past-guides.ts`** — 앱이 써 낸 가이드 원문의 기록. 언어당 1건(2026-08-18 첫 판).
  **템플릿이 아니라 기록**이라, 새 가이드에 맞춰 고치는 것이 아니라 물러난 판을 덧붙인다.
  파일을 따로 뺀 이유는 가이드 하나가 100줄이라 서비스에 두 언어 × 판수만큼 쌓이기 때문이다.
- **`ensureGuide`** — 파일이 있으면 읽어서 기록과 견주고, 우리 것이면 지금 판으로 바꾼다
  (`AC-studio.browser-41`). 줄바꿈과 끝 공백만 눈감는 기존 `sameFile` 판정을 그대로 쓴다.

## 검증

| 무엇 | 결과 |
|---|---|
| `npm run typecheck && npm run lint` | PASS |
| `npm run test` | PASS 705 (49파일) |
| `npm run build` | PASS |
| `npm run test:e2e` | PASS 20 |
| `npm run sweep` | PASS 5/5 |
| `surface-verify` | 해당 없음 — 렌더러 파일 변경 0 |

**실물 확인** — 유저의 실제 워크스페이스로 앱을 띄웠다(그 사람 파일을 고치는 것이 목적이라
이번엔 의도적으로). `export extract` 포함 건수 ko 1 · en 1 → **0 · 0**. `example.ts` 와
`lib/rows.ts` 는 변화 없음.

### 빨강 확인

배선 전에 "앱이 쓴 옛 판을 새 판으로 바꾼다"가 먼저 실패했다 — 옛 판이 그대로 디스크에 남아
있었다. 기존 "고쳐 놓은 가이드는 덮지 않는다" 케이스는 **옛 판 + 메모 한 줄**을 심도록 고쳤다.
아무 문자열이나 심으면 새 장치를 지나가지 않아 방어선 구실을 못 한다.

## 남은 것

- **가이드를 고칠 때마다 `past-guides.ts` 에 옛 원문을 옮겨 적어야 한다.** 빠뜨리면 이 결함이
  그대로 재발한다. 강제하는 검사는 없다.
- 확인용 임시 스크립트가 기본으로 실제 프로파일을 쓰는 문제는 그대로다. 이 결함이 사용자
  화면에 닿은 경로도 그것이었다(앞 회차에서 그 스크립트가 옛 가이드를 그 사람 워크스페이스에
  써 놓았다). `ui-shot`·`surface-verify` 처럼 임시 프로파일을 기본값으로 삼는 것이 맞다.
