/**
 * Connection configuration tab content. Empty stub — implementer fills the
 * left tree, in-use section, new-config dialog, and edit panel.
 *
 * The stub renders only the root container so tests fail on missing testIds
 * / wiring rather than on module resolution.
 */
export function ConnectionConfigTab() {
  return <div data-testid="conn-config-tab-content" />;
}
